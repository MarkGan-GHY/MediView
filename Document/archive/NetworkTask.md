请基于现有的"Network"页面骨架，完成“大模型调用配置页面”的功能实现，语言使用 Kotlin。
注意：**不要大范围重写页面结构，只在现有骨架上补全功能**。

当前页面结构固定为：

1. 顶部：AI功能配置区（开发中，暂不需要添加功能）和预设 Prompt 编辑区（在本次任务需要添加功能）
2. 下方：Column/LazyColumn，元素为 Card，每个 Card 展示一套 API 配置
3. 右下角：添加按钮，用于新增配置


请你依据一下需求，完善Network板块的功能实现，本阶段只需要实现页面逻辑，也就是实现
用户从配置模型，选择模型的闭环
请完成以下功能：

* 设计 API 配置数据模型：id、name、provider、model、endpoint、apiKey、promptTemplate、enabled、isDefault、remark、requestFormatType 等
* 用 ViewModel + UiState + StateFlow 管理页面状态
* 列表展示所有配置，Card 至少显示名称、服务商、模型名、endpoint摘要、是否默认、是否启用、协议类型
* 支持新增配置
* 支持编辑配置
* 支持删除配置，并有确认逻辑
* 支持设置默认配置，且同一时间只能有一个默认项
* 支持启用/禁用配置
* 完善顶部全局预设 Prompt 编辑逻辑，该模块中储存未来用于发送给大模型的Prompt
* 将配置列表和全局 Prompt 本地持久化保存（优先 DataStore 或合理的轻量方案）
* 抽取必要组件，避免把所有逻辑塞在一个文件里
* 参考已有界面进行适当美化，保持现有 UI 风格简洁、工程化、Material 3 风格，不要花哨动画，不要改成多页面复杂流程
* 未来要兼容多厂商 API 格式，因此 requestFormatType 至少支持 OPENAI_COMPATIBLE、QWEN、GEMINI、CUSTOM

实现要求：

* 基于现有项目结构自然落地
* 尽量复用现有代码
* 表单有基础校验
* API Key 支持隐藏显示更佳
* 代码可编译、命名清晰、结构合理
* 完成后在根目录生成任务总结文档，说明修改了哪些文件，以及未来从哪里继续接入真实请求层

说明：
* AI能力配置功能暂时不需要开发，只需要保留配置页面结构，不添加功能就可
* Prompt编辑功能可以预设一套默认的Prompt，用户可以修改，但不能修改为空
* 项目的前置信息可以查看根目录的 手机端设计.txt 文档
